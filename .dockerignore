# =============================================================================
# Docker 构建上下文排除清单
#
# 作用与 .gitignore 不同：.gitignore 管"哪些不进 Git 仓库"，
# 这里管"哪些不进 docker build 的上下文"。仓库里没有的文件（例如被 .gitignore
# 排除的 emoji/）在本地构建时仍然存在，会白白传给构建器，所以两边都要写。
# =============================================================================

# ---------- 版本控制与本地环境 ----------
# `.venv` / `venv` / `env` 刻意**只写裸名**（= 只匹配根目录）：虚拟环境正常就
# 建在仓库根，而 `env` 这个名字太通用，写成 `**/env` 有误伤 `liz_bot/env`
# 这类目录的风险。
.git
.gitignore
.gitattributes
.venv
venv
env

# ---------- 运行产物 ----------
# ⚠️ 这几条**必须带 `**/`**。Docker 的 `*` 与裸名都**不跨 "/"**：
# 裸写 `__pycache__` 只排除根目录那一个，`liz_bot/__pycache__` 照进不误。
# `**/` 也能匹配 0 层，所以 `**/__pycache__` 同时覆盖根目录与所有子目录。
# （2026-09-25 用 moby/patternmatcher 的移植验证后改正，原先的裸名是空转。）
**/__pycache__
**/*.py[cod]
**/*$py.class
**/bot_log
liz_bot/ai_chat
**/*.db

# ---------- 敏感配置（绝不能进镜像层） ----------
# 本地构建时若存在真实 config.yaml，必须排除：镜像层是可被拉取的，
# 写进去等于把 appid/secret 一起发布了。
.env
liz_bot/config/*.yaml
!liz_bot/config/*.example.yaml

# ---------- 体积大且与运行无关的归档 ----------
# legacy_sdgb_stack/ 约 200MB，且含真实二维码与 AES 密钥
legacy_sdgb_stack
legacy_qqbot_stack
_backup
_tools

# maimai/ 是舞萌 DX 服务端 API 客户端（含上号工作流）——
# 一个**功能完整的上传工具**。2026-09-25 用户要求不再发布它，
# 仓库侧已由 .gitignore 的 `maimai/` 移除，这里同步排除。
#
# 为什么镜像里也要排除：`liz_bot/` 对它的 import 数为 **0**，
# 镜像里放它没有任何用处，却会把一个能对官方服务器发包的工具
# 打进**可被拉取的**镜像层 —— 镜像层是公开可下载的，
# 这和把它推到仓库是同一个泄露面。
#
# 下面 `maimai/DLL`、`maimai/baogao` 两条在加了这一条之后已冗余，
# 但刻意保留：它们记着各自的成因（反编译树、密钥报告），
# 万一将来 `maimai/` 需要重新分发，删掉上面那一行即可让它们重新生效。
maimai

# maimai/DLL/ 与 maimai/baogao/ —— 反编译产物与密钥报告，
# 2359 个文件 / 15MB（2356 个 .cs + 2 份报告），是**他人版权**的游戏客户端
# 代码及其分析结果，绝不能进镜像（镜像层是可被拉取的）。
#
# 它已在 .gitignore 里被排除，但**那只管 Git，管不到 docker build 的上下文**。
# 本地构建时这些文件实实在在躺在工作区里，`COPY . .` 会照单全收 ——
# 这正是本节开头那条注释说的情形（.gitignore 与 .dockerignore 各管一边，
# 两边都要写）。2026-09-25 实测发现漏了这条，构建上下文会平白多传 15MB。
#
# 目录被改过名（DLL → baogao），所以两个名字都留着，并额外加一条
# `**/Assembly-CSharp` 防止再改名逃逸。
#
# ⚠️ 这一条原先写的是**裸名** `Assembly-CSharp`，注释还写着"不带路径 = 匹配
# 任意层级" —— 那是 **.gitignore** 的规则，**不是 Docker 的**。Docker 的裸名是
# 精确匹配，`maimai/baogao/Assembly-CSharp` 一条都匹配不上：这条"防改名逃逸"
# 的规则其实是空转，靠的只是上面 `maimai/baogao` 那一条在兜底。2026-09-25
# 用 moby/patternmatcher 的移植验证后改为 `**/` 前缀。
maimai/DLL
maimai/baogao
**/Assembly-CSharp

# ---- 本地采集数据表：既不进仓库，也不进镜像 ----
# `maimai/keychip_results.xlsx` 是**全国机台 Keychip 清单**（3740 行，
# Keychip ID + 店名 + 省份 + 店 ID）。它不属于上面任何一条，体积又只有 168KB，
# 所以两边都漏了 —— 2026-09-25 由本仓 `_tools/simulate_container.py` 的
# "没有漏网的本地文件"检查抓出来（该检查会剪枝 .dockerignore 的排除项，
# 再找**未跟踪且未被排除**的文件）。
#
# 用扩展名通配兜底，别只钉死文件名：这类"采集结果表"下次换个名字还会漏。
**/*.xlsx
**/*.xls
maimai/*.csv

# 两份报告的**文件名**兜底：上面那两条是按目录排的，目录一改名就失效 ——
# 而这两份（密钥与网络 API 报告 / 接口清单）才是真正敏感的东西，
# 用户 2026-09-25 明确说"请勿上传任何平台"。
#
# ⚠️ 必须带 `**/`（裸名是精确匹配，见上文坑 A）。下面 .md 那条其实已被
# `**/*.md` 覆盖，这里显式再列一次是为了让意图可读、且防以后有人删掉那条。
**/maimai_*_api_endpoints.json
**/maimai_*_密钥与网络API报告.md

# ---------- 大体积素材 ----------
# emoji_gif/ 约 34MB，当前代码没有任何引用（保留 emoji/ 那 1MB，
# 它是 pic_haddler 实际读取的目录）
liz_bot/emoji_gif

# ---------- 只读数据（必须进镜像，别在这里加规则）----------
# 下面三个目录都随镜像分发，**不要**为它们添加任何排除规则：
#   liz_bot/divingfish_songs/    曲库（约 900KB）
#   liz_bot/yuzuchan_aliases/    别名库（约 250KB）
#   liz_bot/texts/               回复文本（约 1KB）
#
# 少了前两个，查歌 / 别名指令会**静默**失效（机器人照常启动、探活照常 200，
# 只是回一句"没有找到"）；少了 texts/ 更会让启动自检直接报错退出。
# 尤其别写 `**/*.json` 这类通配规则 —— 它会把这三份数据一起排除掉。
# （裸 `*.json` 只匹配根目录，够不到这三份；但**别指望这个巧合** ——
# 目录哪天挪到根上就中招了。）
# 这几条由 _tools/simulate_container.py 与 verify_deploy_paths.py 把关。

# ---------- 开发与调试杂项 ----------
# 通配规则同样必须带 `**/`：裸 `*.swp` 只能盖到根目录，编辑器在子目录里
# 留下的交换文件就漏进来了。
.idea
.vscode
**/*.swp
**/*.swo
**/*.saz
**/*.reg

# ---------- 文档（一律不进镜像）----------
# 用户 2026-09-25 的要求。文档进镜像没有任何用处（容器里没人读它），却会把
# 仓库的叙述性内容一并发布出去 —— 而镜像层是可被拉取的。
#
# ⚠️ 必须写 `**/*.md`，**不能**写 `*.md`：Docker 的 `*` 不跨 "/"，裸 `*.md`
# 只排除根目录那几份（README.md / DEPLOY_*.md），子目录里的照样会进镜像。
# `**/` 也能匹配 0 层，所以这一条同时覆盖根目录与所有子目录。
#
# （这条规则是靠子目录里的 .md 才暴露出来的：原先单列 `三套SDGB实现对比.md`，
#  它和 `liz_bot/_已移除功能_舞萌发包.md` 都是子目录里的文档。那两份文档
#  2026-09-25 已随文档整理删除，但规则按「任意层级的 .md」写，仍然必要 ——
#  哪天再往子目录放一份文档，不会重新漏进镜像。）
#
# 已核对：仓库里**没有任何代码在运行期读 .md**。liz_bot/*.py 中提到 .md 的
# 地方全在注释 / 文档字符串里，排除它们不影响运行。
**/*.md

# ---------- 本地工作区数据（既不该进镜像，也不该进仓库）----------
# .workbuddy-ai/ 是 AI 助手的工作记录：会话日志、项目笔记、临时分析。
# 里面有部署排错过程、凭据轮换待办等**内部信息**，还有可能被粘进去的
# 半截凭据。它已被 .gitignore 排除，但同样**管不到 docker build** ——
# 不排除就会被打进镜像层，而镜像层是可被拉取的。
.workbuddy-ai

# git bundle（本机打包的完整历史，用于 GitHub 拉不动时 scp 到服务器）。
# 它含**全部历史**，进镜像既没用又是个信息泄露面。同样已在 .gitignore 里。
**/*.bundle

# 本地彩排产生的数据卷（local_rehearsal.py 的默认卷目录）
.local_volume

# 更新公告的**成图**（更新公告_verX.Y.png，约 0.5MB）——
# 它是 `_tools/make_notice_image.py` 渲染出来、**手工发给群友**的发布产物。
# 源文件 .md 已被上面 `**/*.md` 挡住；成图这条单独列，因为
# `**/*.md` 只按扩展名匹配，盖不住 .png。
# 容器里没有任何代码读它（`liz_bot/` 对公告零引用），进镜像纯属白送体积。
# ⚠️ 通配必须带 `**/`（裸名是精确匹配，见上文坑 A）。
**/更新公告_*.png

# deploy/ 是给「自己管操作系统的 VPS」用的：systemd 单元 + docker-compose.yml。
# 容器里没有 systemd、也不会在里面跑 compose，放进去只会让人困惑。
#
# 这一条同时是**安全**相关的：deploy/.env 是容器版的凭据文件，
# 万一它存在（本地试跑过），不进镜像就等于多一层保险 ——
# 镜像层是可被拉取的。_tools/simulate_container.py 的 F 节会验这条。
deploy
