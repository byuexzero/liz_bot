# 容器平台部署（通用）

> **本文件原名 `DEPLOY_CLAWCLOUD.md`。** 最初的目标平台 ClawCloud Run 已于
> **2026-05-11 停服**，故改名为通用文档：平台专属的注册与建应用步骤已删除，
> 保留与平台无关的设计说明、验证方法与自检清单。
>
> **国内平台请直接看 [`DEPLOY_CN.md`](DEPLOY_CN.md)** —— 推荐腾讯云轻量
> （首年 ¥38、无需备案）或阿里云 ECS（¥99/年、续费同价）。

---

## 0. 免费平台的风险不是假设

这份文档的诞生过程本身就是个案例：

| 时间 | 事件 |
|---|---|
| 2025-04 | ClawCloud Run 上线，凭「GitHub 账号满 180 天」每月送 $5 额度，免绑卡 |
| 2026-04-23 | 官方发布停服公告：*"we have made the difficult decision to discontinue our product and related services"* |
| 2026-05-11 | **服务正式停止** |
| 2026-05-20 | 退款截止 |

也就是说：**从公告到彻底下线只有 18 天**，从上线到关停不到 13 个月。
而且它的宣传页在停服后仍然挂得住、搜得到 —— 只看页面不看公告，
很容易把已经死掉的服务当成可用选项。

**三条可操作的教训**：

1. **免费额度是"活动性质"，不是承诺。** 选平台时把"它会消失"当作**正常预期**，
   而不是黑天鹅。
2. **迁移成本要提前压到最低。** 本项目没有任何需要长期保存的业务状态 ——
   曲库随镜像分发且只读，运行期只往卷上写会话历史与日志。
   换平台 = 改几个环境变量。这个特性不是巧合，是设计时刻意保留的
   （见第 2 节），它让你在任何平台出问题时都能**当天走人**。
3. **付费买的不只是资源，还有"不会被随时关掉"这件事。** 一年 ¥38 的
   轻量服务器，比"免费但可能下个月就没了"的额度更省心 —— 这不是价格比较，
   是风险比较。

> 所以：如果你只跑这一个机器人，**别把免费额度当长期方案**。
> 用免费额度验证，用便宜的年付方案承载。

---

## 1. 本机器人的三个硬约束

先明确硬约束，选型才不是碰运气：

| 约束 | 原因 |
|---|---|
| **必须常驻** | botpy 走 WebSocket 长连接，被休眠就等于掉线 |
| **需要可写持久盘** | AI 会话历史与运行日志落在卷上，容器重启就丢 |
| **不需要公网入站** | 只主动连出去到腾讯，本地不监听端口 |

第三条是本项目自己的坑 —— 见下方"为什么要开健康检查端口"。
第二条决定了选平台时的硬性门槛：**不支持挂载持久卷的平台直接排除**。

---

## 2. 挂载持久化卷 + `LIZ_DATA_DIR`（最关键的一步）

**不挂卷 = AI 会话历史与运行日志会在每次重启后消失，而且没有任何报错。**

在平台的存储/卷设置里添加一个卷，挂载路径填：

```
/data
```

容量 1 GB 足够（运行期只写日志与会话记录，日志会轮转）。

然后**必须**设置环境变量 `LIZ_DATA_DIR=/data` 告诉程序卷挂在哪 ——
两者不一致的话程序会继续写容器内的临时目录，等于白挂。

挂卷后路径变成：

```
/data/bot_log/        botpy 运行日志
/data/ai_chat/        AI 会话历史（仅 AI 变体用）
```

曲库（`divingfish_songs/music_data.json`）与别名库
（`yuzuchan_aliases/aliases.json`）**都不在卷上**，它们随镜像分发、**只读**。
自 2026-09-23 起运行期**不再修改仓库内任何文件** ——
歌曲别名的**写入**功能已停用（柚子的别名是众包投票产生的，机器人只读快照），
因此不存在"空卷播种"这一步，空卷只会被建出 `ai_chat/` 与 `bot_log/` 两个目录。

这套逻辑在 `liz_bot/runtime_paths.py`，并有 73 项自动化验证覆盖
（`python _tools/verify_deploy_paths.py`）—— 其中一组专门盯着别名库：
文件是否在位、能否关联上曲库、匹配语义是否为精确匹配、
数字别名会不会劫持 ID 查询；另一组盯着回复文案：键是否齐全、
占位符能否全部替换、文件缺失/损坏/缺键时是否**抛错而不是静默降级**、
改文件后是否**无需重启**即生效。这两组一旦失守，表现都是**静默**的
（机器人照常启动，只是别名指令回一句"没有找到"、或文案改了却毫无变化）。

> ℹ️ 在 **VPS 上这个变量现在只是可选项**：运行期不再写仓库内文件，
> 不设也不会让 `git pull` 冲突。设了则把日志与会话历史挪出仓库，更整洁。
> 详见 [`DEPLOY_CN.md`](DEPLOY_CN.md) 第 5 节。

---

## 3. 环境变量

| 变量 | 必填 | 值 |
|---|---|---|
| `QQ_BOT_APPID` | ✅ | QQ 开放平台的机器人 AppID |
| `QQ_BOT_SECRET` | ✅ | 机器人 AppSecret |
| `LIZ_DATA_DIR` | ✅（挂了卷） | `/data` |
| `HEALTHZ_PORT` | 建议 | `8080` |
| `QIANFAN_ACCESS_KEY` | 仅 AI 变体 | 千帆 Access Key |
| `QIANFAN_SECRET_KEY` | 仅 AI 变体 | 千帆 Secret Key |

缺 `QQ_BOT_APPID` / `QQ_BOT_SECRET` 时程序会打印一行清晰错误并退出
（不是 traceback），在平台日志里一眼能看到。

### 为什么要开健康检查端口

本机器人**不监听任何端口** —— 在裸机/自建 VPS 上没问题，但在容器平台上，
"从不监听端口"的容器很容易被存活探针判为不健康而**反复重启**，
表现为机器人每隔几分钟掉线重连，日志里看不出原因。

所以 `run.py` 会在启动时起一个极小的 HTTP 服务（`liz_bot/healthz.py`），
只做两件事：绑定端口、返回 200。内存开销约 1 MB。

设置 `HEALTHZ_PORT=8080`（或平台自动注入的 `PORT`）即启用；
**不设置则完全空操作**，本地开发与裸机部署不受影响。

启用后在平台的应用设置里把**容器端口 `8080` 暴露出来**（TCP）。
不暴露也不影响机器人收发消息，只是平台侧探活可能不通过。

验证：

```bash
curl https://<平台分配的地址>/
# {"status":"ok","bot":"ready","uptime_seconds":123.4}
```

`bot` 字段在连上腾讯网关后由 `starting` 变为 `ready`，可用来判断真实就绪状态。

> **裸机 / VPS 上不需要这个。** 那里没有平台探针，不设 `HEALTHZ_PORT`
> 就是完全空操作。

---

## 4. 部署后验证

1. **看日志**：启动时应能看到类似

   ```
   数据来源：数据卷 /data
   AI 会话：/data/ai_chat
   日志目录：/data/bot_log
   曲库基线：/app/liz_bot/divingfish_songs
   别名基线：/app/liz_bot/yuzuchan_aliases
   回复文本：/app/liz_bot/texts
   健康检查已监听 0.0.0.0:8080
   ```

   `数据来源` 必须是"数据卷 /data" —— 显示"仓库内目录"说明 `LIZ_DATA_DIR` 没生效。
   最后一行 `回复文本` 对应 `texts/replies.json`；文件缺失时 `run.py` 会在打印
   这几行**之前**就报错退出，所以能看到这 6 行就说明文案文件齐备。

2. **群内发指令**：`@机器人 /help`，再试 `/id 某首歌`。

3. **验持久化**：让机器人产生一些会话记录 / 日志，然后在平台上重启容器，
   重启后卷上的 `bot_log/`、`ai_chat/` 内容仍在。
   这一步别跳过 —— 它验证的正是第 2 节那个最容易漏的配置。

---

## 5. 更新部署

改完代码推到 GitHub，在平台上重新构建/部署即可。

容器重启不会丢数据（前提是第 2 节做对了），因为数据在卷上而不在容器里。

---

## 6. 已知风险与备份

| 风险 | 说明 | 应对 |
|---|---|---|
| **平台停服** | 免费/小平台可能随时下线（见第 0 节） | 数据随时可迁走；重要数据本地留一份 |
| 容器偶发停止 | 部分平台会自行停止容器 | 定期看一眼控制台；`bot` 字段可辅助判断 |
| 免费额度是活动性质 | 官方通常不承诺截止日期 | 关注官网公告；别当长期方案 |
| 数据丢失 | 卷也可能出问题 | 定期备份卷内目录 |

**备份卷内数据**（日志 + 会话历史，体积不大，值得每天备一次）：

```bash
# 用平台的文件管理/终端功能导出，或直接下载卷内文件
```

> 迁移成本很低：本项目除卷上的日志与会话历史外无任何状态。
> 换平台 = 改几个环境变量。所以**不要**因为"已经部署了"
> 就容忍一个不稳定的环境 —— 说走就走是这套设计的核心价值。

---

## 7. 本地用 Docker 复现

本机没装 Docker 也能先验证逻辑（`python _tools/verify_deploy_paths.py`）。
装了 Docker 后可以完整复现线上形态：

```bash
docker build -t liz-bot .

# 模拟线上：挂卷 + 开健康检查端口
docker run --rm \
  -v liz-data:/data \
  -e LIZ_DATA_DIR=/data \
  -e HEALTHZ_PORT=8080 \
  -p 8080:8080 \
  -e QQ_BOT_APPID=你的AppID \
  -e QQ_BOT_SECRET=你的AppSecret \
  liz-bot
```

另开一个终端：

```bash
curl http://127.0.0.1:8080/
```

> 没有 Docker 也行 —— `python _tools/simulate_container.py` 会用
> `git archive` + `.dockerignore` 复现构建上下文并真跑一遍入口，
> 不需要 Docker 守护进程。详见 [`DEPLOY_LOCAL.md`](DEPLOY_LOCAL.md)。

---

## 8. 部署前自检（强烈建议先跑）

四个脚本都在 `_tools/`，只依赖标准库，可在任何 Python 3.10+ 上跑。
它们验证的是**部署配置**而非业务逻辑 —— 因为这类错误的表现都是**静默失败**，
在生产环境里极难排查。

```bash
# 0. 本地彩排：用「云端的方式」（持久化卷 + 探活端口）在本地跑一遍
#    跑两次即可验证「重启后卷上数据不丢」—— 详见 DEPLOY_LOCAL.md
python _tools/local_rehearsal.py

# 1. 持久化卷与 LIZ_DATA_DIR 是否配对 —— 最该跑的一个
python _tools/verify_deploy_paths.py

# 2. 模拟容器构建：.dockerignore 有没有误伤运行必需文件
python _tools/simulate_container.py

# 3. 依赖在 linux/amd64 上是否需要源码编译（决定 Dockerfile 要不要 build-essential）
python _tools/check_wheels.py
```

**第 0 个最有说服力**：它把第 3 节的两个变量按云端的取值设好再启动，
并真的连一次 QQ —— 本地能看到 `bot: ready`，云端就没有理由看不到。
**第 1 个最重要**：`LIZ_DATA_DIR` 与卷挂载点不一致时，程序会继续写容器内的
临时目录 —— 日志与会话历史每次重启都丢，而且**不报任何错**。

> 注意用 **Python 3.10** 跑（`qq-botpy` 只装在那上面）。脚本会自动探测解释器，
> 但如果你手动指定了别的版本，可能误报 `ModuleNotFoundError`。

---

## 附：平台选型

| 方案 | 结论 |
|---|---|
| **国内轻量服务器 / ECS** | **首选**，见 [`DEPLOY_CN.md`](DEPLOY_CN.md)。腾讯云轻量首年 ¥38、阿里云 ECS ¥99/年 续费同价。**无需 ICP 备案**（不绑域名、不提供网站、不需要公网入站），且比按量容器便宜 2～8 倍 |
| ClawCloud Run | ❌ **已于 2026-05-11 停服** |
| Render 免费 | 15 分钟**无入站流量**即休眠 —— WebSocket 出站消息不算入站，一停就断线；免费层无后台 Worker；文件系统临时 |
| Oracle Always Free | 内存/成本最优（2 核 ARM / 12 GB，¥0），但需绑卡验证、热门区 ARM 常缺货 |
| Fly.io | 约 ¥16/月，架构最贴合（真 VM + 真持久卷），但已无免费额度、必须绑卡 |
| Sealos | 国内按量容器平台，支持持久卷。但按量计费的省钱机制（缩容到 0）**对长连接不适用**，1核2G 常驻约 ¥307/年 |
| CloudBase 云托管 / SAE | 国内按量容器，1核2G 常驻约 ¥1700+/年，**明显不划算** |

**选型的核心判断**：本项目必须 24×7 保持长连接，所以"按量计费能缩容到 0"
这个省钱机制**完全用不上** —— 无论如何都要付满 720 小时/月。
既然必须付满，**包年**的轻量服务器必然更便宜。详细推算见
[`DEPLOY_CN.md`](DEPLOY_CN.md) 第 1 节。
