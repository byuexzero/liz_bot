# ==========================================================================
# 日志
# ==========================================================================
# botpy 日志统一存放在 bot_log/（由 liz_bot/qqgroupbot.py 指定）
bot_log/
liz_bot/botpy.log*

# ==========================================================================
# 运行产物
# ==========================================================================
__pycache__/
*.py[cod]
*$py.class
*.db

# AI 会话历史（liz_bot/qqgroup-ai-bot.py 写入）。
#
# 这里存的是**真实的群聊对话内容**，属于用户数据，不该进仓库。
# 没有这条规则时它不会报错、也不会被 .dockerignore 拦下 —— 只是静静地
# 出现在 `git status` 的未跟踪列表里，一次 `git add -A` 就提交上去了。
# 注意写 `ai_chat/`（带斜杠、无前导路径）而不是 `liz_bot/ai_chat/`：
# 前者匹配任意层级，同时覆盖容器版的 deploy/data/ai_chat/。
ai_chat/

# 容器版的数据卷（deploy/docker-compose.yml 的 ./data:/data）。
# 里面是日志与会话历史；bot_log/ 那条已覆盖日志，这条兜住其余内容。
deploy/data/

# ==========================================================================
# 虚拟环境
# ==========================================================================
.venv/
venv/
env/

# ==========================================================================
# IDE / 编辑器
# ==========================================================================
.idea/
.vscode/
*.swp
*.swo

# ==========================================================================
# 敏感配置 —— 绝不入库
# ==========================================================================
# 运行时配置。含可直接上号的凭据（AES 密钥、userId、扫卡二维码），
# 等同账号密码。模板见 .env.example。
.env

# QQ 机器人配置。含真实 appid / secret，泄露即可冒用机器人身份。
#
# 配置已改为优先读环境变量（QQ_BOT_APPID / QQ_BOT_SECRET，见 liz_bot/config.py），
# 下面的 YAML 只是本地开发的回退，云端部署无需提供。
#
# 必须反排除 *.example.yaml —— 它们是模板，应当入库。
# 少了这一行，`liz_bot/config/*.yaml` 会把模板一起吞掉（曾真实发生）。
liz_bot/config/*.yaml
!liz_bot/config/*.example.yaml

# ==========================================================================
# 旧实现存档 —— 不上传
# ==========================================================================
# legacy_sdgb_stack/ 内含 sdgb / eaquira / Lionheart 三套旧实现（约 200MB），
# 且 eaquira 的 settings.py 有真实二维码、encrypt.py 有 AES 密钥。
legacy_sdgb_stack/

# 旧 QQ 技术栈（go_cqhttp + unidbg-fetch-qsign）的重建说明与残留
legacy_qqbot_stack/

# 兼容旧路径（目录已迁入 legacy_sdgb_stack/）
eaquira/
sdgb/
eaquira-master/

# ==========================================================================
# 调试 / 开发工具 —— 不上传
# ==========================================================================
# _tools/ 内含类型转换器与各套验证脚本，属开发期工具，不随项目分发。
#
# 例外：六个脚本随仓库分发。前四个是**部署自检脚本**，它们的共同点是能在
# 部署前发现问题，而对应的问题在运行时表现为"静默丢数据"或"容器起来才炸"，
# 极难排查：
#   verify_deploy_paths.py   持久化卷与 LIZ_DATA_DIR 是否配对
#   simulate_container.py    .dockerignore 有没有误伤运行必需文件
#   check_wheels.py          依赖在 linux/amd64 上是否需要源码编译
#   local_rehearsal.py       用「云端的方式」在本地跑一遍（卷 + 探活端口）
#
# 后两个是**数据更新脚本** —— 曲库与别名库都需要人工更新，脚本必须能拿到：
#   fetch_music_data.py      从水鱼拉取完整乐曲信息到本地
#   fetch_aliases.py         从柚子拉取完整歌曲别名到本地
#
# 注意必须写成 `_tools/*` 而不是 `_tools/`：Git 无法在**目录被整体排除**时
# 反排除其中的文件，只能"排除目录内容 + 单独放行文件"。
_tools/*
!_tools/verify_deploy_paths.py
!_tools/simulate_container.py
!_tools/check_wheels.py
!_tools/local_rehearsal.py
!_tools/fetch_music_data.py
!_tools/fetch_aliases.py

# 本地彩排产生的数据卷（local_rehearsal.py 的默认卷目录）—— 不上传
.local_volume/

# ==========================================================================
# 备份 —— 不上传
# ==========================================================================
_backup/

# ==========================================================================
# 素材与数据
# ==========================================================================
# 表情包 GIF（约 34MB）与 PNG（约 1MB）。体积大、非代码，
# 克隆后自行补齐即可，机器人缺了它们只是少几个表情指令。
liz_bot/emoji_gif/
liz_bot/emoji/

# 歌曲数据库（music_data.json 约 900KB）**随仓库分发**：
# 查歌是核心功能，缺了它 /id、/song、/name 等指令全部不可用。
# 数据源为水鱼（diving-fish）公开接口，由 _tools/fetch_music_data.py 拉取。
# 注意：此处刻意**不**忽略 liz_bot/divingfish_songs/ 本身。
#
# 只有 ETag 缓存不随仓库分发 —— 它是本机增量更新用的，换台机器就该重新协商。
liz_bot/divingfish_songs/*.etag

# 别名库（aliases.json 约 250KB）同样**随仓库分发**：
# /别名查歌、/查询别名、/cbm 以及 /song 的别名兜底都依赖它，
# 缺了它这些指令会静默返回"没有找到"。
#
# 数据源为柚子（yuzuchan）公开接口，由 _tools/fetch_aliases.py 拉取 ——
# 水鱼**不提供**歌曲别名，别名由柚子单独维护（玩家众包投票产生）。
# 同样刻意**不**忽略 liz_bot/yuzuchan_aliases/ 本身。
#
# 注意：柚子不发 ETag / Last-Modified，协商式缓存对它无效（实测），
# 所以 .etag 基本不会生成；fetch_aliases.py 改用"内容一致即不改写"
# 来保证重复拉取无副作用。这条规则留着以防上游日后补上 ETag。
liz_bot/yuzuchan_aliases/*.etag

# 回复文本（texts/replies.json，约 1KB）同样**随仓库分发**：
# 全部用户可见文案（未找到、未知指令、帮助、问候、查歌排版模板……）都在这里，
# 改文案不用碰代码。缺了它机器人**起不来** —— 启动自检会直接报错退出
# （刻意不内置代码兜底，避免"改了文件却没生效"）。
#
# 注意它**不在** liz_bot/config/ 下：config/ 的 yaml 被排除（只放行
# *.example.yaml），而文本文件必须随仓库与镜像分发。
# 同样刻意**不**忽略 liz_bot/texts/ 本身，也**不要**加 `*.json` 这类通配规则。
# （本目录没有 ETag 缓存，所以这里没有对应的忽略行 —— 是刻意的，不是漏写。）

# 旧曲库数据集（maimaiDX-songs）与旧别名表（alias.json）已于 2026-09-23
# 整体归档到 _backup/，liz_bot/maimaiDX_songs/ 已不存在，
# 因此这里不再需要针对该目录的任何忽略规则。
# 归档说明（含回退步骤）见 _backup/liz_bot_song_db_2026-09-23/README.md。

# ==========================================================================
# AI 工作区数据
# ==========================================================================
.workbuddy-ai/

# ==========================================================================
# 抓包 / 调试产物
# ==========================================================================
*.saz

# git bundle 是打包产物（用于"GitHub 拉不动时从本机 scp 到服务器"）。
# 别提交它 —— 它含完整历史但会过期。
*.bundle
_commitmsg.tmp

# ==========================================================================
# 部署说明文档 —— 只在本地保留，不随仓库分发
# ==========================================================================
# 这三份是部署者自己的 runbook，含具体主机路径与逐步操作，面向公开分发
# 没有意义（也和 AGPL「提供对应源码」的义务无关 —— 那不是源码）。
#
# 注意：**光写 .gitignore 是不够的** —— 这些文件在本规则加进来之前
# 已被 git 跟踪，.gitignore 对已跟踪文件无效。必须配一次
# `git rm --cached <file>` 才能真正从仓库移除（本地文件保留）。
# 只加规则不 rm，会出现"规则写了、文件照样被提交"的假象。
DEPLOY_CN.md
DEPLOY_LOCAL.md
DEPLOY_PAAS.md
maimai/README.md

# 部署产物本身**仍然随仓库分发**（deploy/ 下的 systemd 单元、
# docker-compose.yml、.env.example）—— 它们是可执行的配置，不是说明文档。
# 注意 deploy/ 整体被 .dockerignore 排除，但**不在** .gitignore 里。

# ==========================================================================
# 第三方游戏程序集 —— 绝不入库、绝不外发
# ==========================================================================
# maimai/DLL/Assembly-CSharp.dll 是舞萌 DX（Unity）客户端编译出的
# C# 程序集（二进制里含 UnityEngine / mscorlib / maimai 标记）。
# 那是**他人版权的游戏代码**，随公开仓库分发会构成侵权，也可能违反
# 游戏服务条款。仓库里**没有任何代码引用它**，删掉不影响机器人运行。
#
# 2026-09-25 用户明确要求"请勿上传任何平台"。这份文件当时是未跟踪状态，
# 但**没有**任何忽略规则 —— 一次 `git add -A` 就会把它卷进提交并推到
# 公开仓库。补上这条规则堵住这个口子。
#
# 注意 `git add -A` 会扫进所有未忽略的未跟踪文件，所以**新建这类文件后
# 第一件事就是确认它在 git status 里不出现**，别等到 push 前才想起来。
#
# 2026-09-25 补记：用户把 `maimai/DLL/` **改名**成了 `maimai/baogao/`
# （15MB / 2359 文件，内含 Assembly-CSharp/ 反编译树 + 2 份密钥与 API 报告）。
# **改名即逃逸** —— 原来那条 `maimai/DLL/` 一条都匹配不上，
# `git status` 立刻把它显示为未跟踪、且未被忽略，一次 `git add -A` 就会
# 推到公开仓库。所以这里同时列三个路径/名字，别再只写一个具体目录名。
#
# `Assembly-CSharp/` 不带前导路径，按 gitignore 规则会匹配**任意层级**，
# 这样以后无论把它挪到哪儿（或再反编译一次换个目录名）都还在网里。
maimai/DLL/
maimai/baogao/
Assembly-CSharp/

# ---- 两份报告：比反编译代码更要紧，必须按**文件名**兜底 ----
# 上面 `Assembly-CSharp/` 只护得住反编译树。这两份是**独立文件**，
# 一旦目录被改名就一起逃逸 —— 而它们才是真正含密钥 / 接口清单的那部分，
# 泄露后果比那 2356 个 .cs 更严重。
#
# 用户 2026-09-25 明确指示：`maimai/baogao/` 下的文件**请勿上传任何平台**。
#
# gitignore 里不带前导路径的名字本来就匹配任意层级，所以写裸名即可。
maimai_1.55_api_endpoints.json
maimai_1.55_密钥与网络API报告.md
# 版本号会变（1.55 → 1.56 …），所以再加两条通配，别只钉死这一版。
maimai_*_api_endpoints.json
maimai_*_密钥与网络API报告.md

# ==========================================================================
# 本地采集数据 —— 绝不入库、绝不进镜像
# ==========================================================================
# `maimai/keychip_results.xlsx`（约 168KB）是**全国机台 Keychip 清单**：
# 3740 行，每行一个 Keychip ID + 店名 + 省份 + 店 ID
# （形如 A63E01C2562 / 嗨森汇乐园成都金牛凯德店 / 四川 / 1320）。
#
# 2026-09-25 由 `_tools/simulate_container.py` 的"漏网本地文件"检查抓出来：
# 它当时既未被 .gitignore 忽略、也未被 .dockerignore 排除，而且是**未跟踪**
# 状态 —— 也就是说 `git add -A` 会把它提交并推到公开仓库，
# `docker build` 会把它打进**可被拉取的**镜像层。
#
# 这类"采集结果表"的共同特征是：体积不大、名字不像凭据、所以两边都容易漏。
# 故按**扩展名通配**兜底，而不是只钉死这一个文件名。
maimai/*.xlsx
maimai/*.xls
maimai/*.csv
# 反编译产物之外的临时缓存（例如 fetch_music_data.py 拉下来的曲库快照）
maimai/_*_tmp.json
_tools/_*_tmp.json

# ==========================================================================
# 服务端 API 工具链 —— 只在本地保留，不随仓库分发（2026-09-25）
# ==========================================================================
# `maimai/` 是舞萌 DX 服务端 API 客户端：加密层、扫码、用户数据容器、
# **上号工作流**。它是一个**功能完整的上传工具** —— 能构造并发送与官方
# 服务器通信的请求，即"把成绩写到游戏账号上"。
#
# 用户 2026-09-25 明确要求：**不要继续发布这个包。**
# 理由不是体积（21 个文件），而是它的能力本身 —— 公开分发一个可直接对
# 官方服务器发包的完整工具，既可能违反游戏服务条款，也让仓库的性质从
# "聊天机器人"变成"作弊工具"。前者是个人自用，后者是给所有人发枪。
#
# ⚠️ 本包**与机器人运行无关**：`liz_bot/` 对它的 import 数为 **0**
# （2026-09-25 全仓核对）。`run.py` / `download.py` 也不引用它。
# 所以移出仓库不影响机器人任何功能。
# 注意别和 `command_router.py` 里的 `maimai.unparsed` 文案键搞混 ——
# 那是 `#` 前缀命名空间的**回复文本**，与本包毫无关系，必须保留。
#
# 下面的 `maimai/DLL/`、`maimai/baogao/`、`maimai/*.xlsx` 等规则
# 在加了这一条之后**已经是冗余**了，但刻意保留：
# 它们是按"最坏情况"逐条排查出来的（反编译树、密钥报告、机台清单），
# 注释里记着各自的成因。万一将来 `maimai/` 又需要分发，
# 这一条一删，那些细粒度规则立刻重新生效。
#
# 恢复分发：删掉下面这一行，再 `git add maimai/` 即可（本地文件一直都在）。
maimai/
