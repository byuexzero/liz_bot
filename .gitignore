# ==========================================================================
# 日志
# ==========================================================================
# botpy 日志统一存放在 bot_log/（由 liz_bot/qqgroupbot.py 指定）
bot_log/
liz_bot/botpy.log*

# ==========================================================================
# 运行产物
# ==========================================================================
__pycache__/
*.py[cod]
*$py.class
*.db

# ==========================================================================
# 虚拟环境
# ==========================================================================
.venv/
venv/
env/

# ==========================================================================
# IDE / 编辑器
# ==========================================================================
.idea/
.vscode/
*.swp
*.swo

# ==========================================================================
# 敏感配置 —— 绝不入库
# ==========================================================================
# 运行时配置。含可直接上号的凭据（AES 密钥、userId、扫卡二维码），
# 等同账号密码。模板见 .env.example。
.env

# QQ 机器人配置。含真实 appid / secret，泄露即可冒用机器人身份。
# 模板见 liz_bot/config/*.example.yaml。
liz_bot/config/*.yaml

# ==========================================================================
# 旧实现存档 —— 不上传
# ==========================================================================
# legacy_sdgb_stack/ 内含 sdgb / eaquira / Lionheart 三套旧实现（约 200MB），
# 且 eaquira 的 settings.py 有真实二维码、encrypt.py 有 AES 密钥。
legacy_sdgb_stack/

# 旧 QQ 技术栈（go_cqhttp + unidbg-fetch-qsign）的重建说明与残留
legacy_qqbot_stack/

# 兼容旧路径（目录已迁入 legacy_sdgb_stack/）
eaquira/
sdgb/
eaquira-master/

# ==========================================================================
# 调试 / 开发工具 —— 不上传
# ==========================================================================
# _tools/ 内含类型转换器与各套验证脚本，属开发期工具，不随项目分发。
_tools/

# ==========================================================================
# 备份 —— 不上传
# ==========================================================================
_backup/

# ==========================================================================
# 素材与数据 —— 不上传
# ==========================================================================
# 表情包 GIF（约 34MB）与 PNG（约 1MB）。体积大、非代码，
# 克隆后自行补齐即可，机器人缺了它们只是少几个表情指令。
liz_bot/emoji_gif/
liz_bot/emoji/

# 歌曲数据库（约 2MB），来自上游 maimaiDX-songs 数据集，属第三方内容。
# 缺失会导致 /id 等歌曲查询指令不可用。
# 恢复方式见根目录 README.md「缺失素材」一节。
liz_bot/maimaiDX_songs/

# ==========================================================================
# AI 工作区数据
# ==========================================================================
.workbuddy-ai/

# ==========================================================================
# 抓包 / 调试产物
# ==========================================================================
*.saz
