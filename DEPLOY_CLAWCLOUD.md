# 部署到 ClawCloud Run

面向本项目的 ClawCloud Run（爪云）部署说明。目标读者是"知道怎么跑 Python，
但没在这个平台上部署过"的人。

> 平台 UI 文案与计费口径会随版本变化。本文标注了核实时间（2026-09），
> **下单前请以官网实时页面为准**。涉及金额的一律按官网当次抓取计算。

---

## 0. 为什么是它

先明确本机器人的三个硬约束，选型才不是碰运气：

| 约束 | 原因 |
|---|---|
| **必须常驻** | botpy 走 WebSocket 长连接，被休眠就等于掉线 |
| **需要可写持久盘** | 用户新增的别名写在 `alias.json` 上，容器重启就丢 |
| **不需要公网入站** | 只主动连出去到腾讯，本地不监听端口 |

ClawCloud Run 对上前两条：免费实例**没有强制冷启动休眠**，且支持挂载
持久化数据卷。第三条则是本项目自己的坑 —— 见下方"为什么要开健康检查端口"。

**它不适合的场景**：把这里当唯一的生产环境。平台较年轻、免费额度是活动性质，
容器也偶发自行停止。关键数据请配合备份（见第 9 节）。

---

## 1. 前置条件

1. **一个注册满 180 天的 GitHub 账号。**
   未满 180 天只能拿一次性 $5 额度，不会每月续。这是免绑卡拿到长期免费额度的门槛。
2. 本仓库已推送到该账号下的 GitHub 仓库。
3. 确认仓库里**没有** `liz_bot/config/*.yaml`（含真实 appid/secret）。
   `.gitignore` 已排除，但值得自己再确认一次：

   ```bash
   git ls-files | grep -E 'config/.*\.yaml$'   # 只应看到 *.example.yaml
   ```

---

## 2. 登录与选区域

官方入口（注意域名带区域前缀，这是东京节点）：

```
https://ap-northeast-1.run.claw.cloud/signin
```

用 GitHub 账号授权登录。注册时可选的区域一般有：新加坡、日本、美东、美西、德国。

**选哪个**：本机器人是纯出站长连接，选**日本（东京）**延迟最低，其次新加坡。
网上有"日本节点自定义域名证书申请有问题"的说法 —— 那是绑定自有域名时才会遇到，
本项目不绑域名，不受影响。

---

## 3. 创建应用

在控制台里创建一个应用（界面上叫 App Launchpad / Create App 之类），要点：

| 项 | 取值 |
|---|---|
| 来源 | 你的 GitHub 仓库 |
| 构建方式 | **Dockerfile**（仓库根目录已有，无需额外配置） |
| 分支 | `main` |
| 启动命令 | 不用填，`Dockerfile` 里的 `CMD` 已指定 `python run.py` |

平台会自动识别根目录的 `Dockerfile`。首次构建需要几分钟 ——
镜像里要装 `build-essential` 来编译少数没有预编译 wheel 的依赖，
装完即卸，不会留在最终镜像里。

---

## 4. 挂载持久化卷（最关键的一步）

**不挂卷 = 用户攒的别名会在每次重启后消失，而且没有任何报错。**

在应用的存储/卷设置里添加一个卷，挂载路径填：

```
/data
```

容量 1 GB 足够（别名表约 360 KB，日志会轮转）。

然后**必须**设置环境变量 `LIZ_DATA_DIR=/data` 告诉程序卷挂在哪 ——
两者不一致的话程序会继续写容器内的临时目录，等于白挂。

挂卷后路径变成：

```
/data/alias.json      用户新增的别名（唯一需要保住的文件）
/data/bot_log/        botpy 运行日志
/data/ai_chat/        AI 会话历史（仅 AI 变体用）
```

曲库文件（`songs.json` 等）**不在卷上**，它们随镜像分发、只读。
首次启动时程序会把镜像内的基线 `alias.json` 播种到空卷上，避免挂载后
"别名突然全空"；之后卷上的版本始终优先，**不会被镜像覆盖**。
这套逻辑在 `liz_bot/runtime_paths.py`，并有 32 项自动化验证覆盖
（`python _tools/verify_deploy_paths.py`）。

---

## 5. 环境变量

| 变量 | 必填 | 值 |
|---|---|---|
| `QQ_BOT_APPID` | ✅ | QQ 开放平台的机器人 AppID |
| `QQ_BOT_SECRET` | ✅ | 机器人 AppSecret |
| `LIZ_DATA_DIR` | ✅（挂了卷） | `/data` |
| `HEALTHZ_PORT` | 建议 | `8080` |
| `QIANFAN_ACCESS_KEY` | 仅 AI 变体 | 千帆 Access Key |
| `QIANFAN_SECRET_KEY` | 仅 AI 变体 | 千帆 Secret Key |

缺 `QQ_BOT_APPID` / `QQ_BOT_SECRET` 时程序会打印一行清晰错误并退出
（不是 traceback），在平台日志里一眼能看到。

### 为什么要开健康检查端口

本机器人**不监听任何端口** —— 在裸机/自建 VPS 上没问题，但在容器平台上，
"从不监听端口"的容器很容易被存活探针判为不健康而**反复重启**，
表现为机器人每隔几分钟掉线重连，日志里看不出原因。

所以 `run.py` 会在启动时起一个极小的 HTTP 服务（`liz_bot/healthz.py`），
只做两件事：绑定端口、返回 200。内存开销约 1 MB。

设置 `HEALTHZ_PORT=8080`（或平台自动注入的 `PORT`）即启用；
**不设置则完全空操作**，本地开发与裸机部署不受影响。

启用后在平台的应用设置里把**容器端口 `8080` 暴露出来**（TCP）。
不暴露也不影响机器人收发消息，只是平台侧探活可能不通过。

验证：

```bash
curl https://<平台分配的地址>/
# {"status":"ok","bot":"ready","uptime_seconds":123.4}
```

`bot` 字段在连上腾讯网关后由 `starting` 变为 `ready`，可用来判断真实就绪状态。

---

## 6. 资源配置与成本

平台计费口径（2026-09 抓取）：

| 资源 | 单价 |
|---|---|
| vCPU | $4 / 月 |
| 内存 | $2 / GB / 月 |
| 磁盘 | $0.12 / GB / 月 |
| 流量 | $0.05 / GB |

本项目实测 RSS 峰值 **41.7 MB**（`_tools/measure_memory.py`），
CPU 几乎全程空闲。推荐配置：

| 项 | 建议值 | 月成本 |
|---|---|---|
| vCPU | 0.1 | $0.40 |
| 内存 | 256 MB | $0.50 |
| 磁盘 | 1 GB | $0.12 |
| **合计** | | **约 $1.02** |

对照每月 $5 赠金，**用掉约 20%，余量充足**（约 4 倍）。
免费层另有硬上限：最多 4 个容器、4 vCPU / 8 GiB 内存 / 10 GiB 磁盘 / 10 GB 流量。
本机器人的流量极小（文本指令 + 偶发图片上传），10 GB 远远够用。

---

## 7. 部署后验证

1. **看日志**：启动时应能看到类似

   ```
   数据来源：数据卷 /data
   别名表：/data/alias.json
   AI 会话：/data/ai_chat
   日志目录：/data/bot_log
   曲库基线：/app/liz_bot/maimaiDX_songs
   空卷首次启动，已播种基线别名表 → /data/alias.json
   健康检查已监听 0.0.0.0:8080
   ```

   `数据来源` 必须是"数据卷 /data" —— 显示"仓库内目录"说明 `LIZ_DATA_DIR` 没生效。

2. **群内发指令**：`@机器人 /help`，再试 `/id 某首歌`。

3. **验持久化**：加一个别名，然后在平台上重启容器，重启后该别名仍在。
   这一步别跳过 —— 它验证的正是第 4 节那个最容易漏的配置。

---

## 8. 更新部署

改完代码推到 GitHub，在平台上重新构建/部署即可。

容器重启不会丢数据（前提是第 4 节做对了），因为数据在卷上而不在容器里。

---

## 9. 已知风险与备份

| 风险 | 说明 | 应对 |
|---|---|---|
| 容器偶发停止 | 社区反馈该平台容器会自行停止 | 定期看一眼控制台；`bot` 字段可辅助判断 |
| 免费额度是活动性质 | 官方未承诺截止日期 | 关注官网公告；随时可迁走（见下） |
| 平台较年轻 | 长期可用性未经时间检验 | **不要**只存一份数据 |
| 数据丢失 | 卷也可能出问题 | 定期备份 `alias.json` |

**备份 `alias.json`**（约 360 KB，值得每天备一次）：

```bash
curl -s https://<平台地址>/ > /dev/null   # 探活（可选）
# 用平台的文件管理/终端功能导出，或直接下载卷内文件
```

> 迁移成本很低：本项目除 `alias.json` 外无任何状态。
> 换平台 = 改几个环境变量 + 拷一个文件。所以**不要**因为"已经部署了"
> 就容忍一个不稳定的环境。

---

## 10. 本地用 Docker 复现

本机没装 Docker 也能先验证逻辑（`python _tools/verify_deploy_paths.py`）。
装了 Docker 后可以完整复现线上形态：

```bash
docker build -t liz-bot .

# 模拟线上：挂卷 + 开健康检查端口
docker run --rm \
  -v liz-data:/data \
  -e LIZ_DATA_DIR=/data \
  -e HEALTHZ_PORT=8080 \
  -p 8080:8080 \
  -e QQ_BOT_APPID=你的AppID \
  -e QQ_BOT_SECRET=你的AppSecret \
  liz-bot
```

另开一个终端：

```bash
curl http://127.0.0.1:8080/
```

---

## 11. 部署前自检（强烈建议先跑）

三个脚本都在 `_tools/`，只依赖标准库，可在任何 Python 3.10+ 上跑。
它们验证的是**部署配置**而非业务逻辑 —— 因为这类错误的表现都是**静默失败**，
在生产环境里极难排查。

```bash
# 1. 持久化卷与 LIZ_DATA_DIR 是否配对 —— 最该跑的一个
python _tools/verify_deploy_paths.py

# 2. 模拟容器构建：.dockerignore 有没有误伤运行必需文件
#    不需要 Docker：用 git 内容 + .dockerignore 规则复现镜像内容，
#    再在模拟镜像里真跑一遍入口、卷播种与查歌链路
python _tools/simulate_container.py

# 3. 依赖在 linux/amd64 上是否需要源码编译（决定 Dockerfile 要不要 build-essential）
python _tools/check_wheels.py
```

**第 1 个最重要。** `LIZ_DATA_DIR` 与卷挂载点不一致时，程序会继续写容器内的
临时目录 —— 别名表每次重启都丢，而且**不报任何错**。这正是第 4 节反复强调
两者必须一致的原因。

> 注意用 **Python 3.10** 跑（`ijson` 只装在那上面）。脚本会自动探测解释器，
> 但如果你手动指定了别的版本，可能误报 `ModuleNotFoundError`。

---

## 附：为什么不选其他方案

| 方案 | 结论 |
|---|---|
| Render 免费 | 15 分钟**无入站流量**即休眠 —— WebSocket 出站消息不算入站，一停就断线；免费层无后台 Worker；文件系统临时 |
| Oracle Always Free | 内存/成本最优（2 核 ARM / 12 GB，¥0），但需绑卡验证、热门区 ARM 常缺货；可作为备选或双活 |
| Fly.io | 约 ¥16/月，架构最贴合（真 VM + 真持久卷），但已无免费额度、必须绑卡 |
| 腾讯云轻量 | ¥30–32/月（年付），要发票/中文工单选它；需实名认证 |

本项目是**出站长连接**，多几十毫秒延迟人类无感 —— 所以"必须用国内机房"
是伪需求。真正要关心的是**平台会不会休眠**和**长连接稳不稳**。
