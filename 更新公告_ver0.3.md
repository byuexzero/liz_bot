【Liz 更新公告 · ver.0.3】

大家好呀～ 这次上新一个指令：/估分。
另外把上次留的那个「表格在手机上可能歪」的尾巴从根上解决了，
顺带修了两个小毛病，其中一个是我自己上次埋的。

──────────────────
① /songdata 现在直接发图
──────────────────
以前判定细节是空格对齐的纯文本，手机字体不等宽时表格会歪。
现在直接画成图片发出来 —— 对齐和配色都是确定的，不会再走形：

  /songdata <歌曲id> <难度>
  难度写 0-4，或写 绿/黄/红/紫/白

扣分用颜色区分轻重：绿色是轻的，琥珀色偏重，红色最重。
break 的 8 个档位（100p/75p/50p/great1~3/good/miss）也一并画在下面。

※ 万一你的群收不到图（比如图片功能没开通），Liz 会自动退回
  文字版，功能一样用，不会报错。

──────────────────
② 参数给错了，不用重打整条指令
──────────────────
上次加了「缺参数会问你」，但有个毛病：补的参数如果不对（歌查不到、
难度写错），Liz 会把这次补参直接丢掉，你得把整条指令重打一遍 ——
正好是补参想省掉的那件事。

现在报错之后直接再发一次就行：

  你：/songdata
  Liz：还差 2 个参数（歌曲id 难度）～ 直接发给我就好
  你：99999 紫
  Liz：Liz没有找到这样的歌
        重新发一次就好～ 需要：歌曲id 难度
  你：143 紫
  Liz：（判定细节）

※ 重来时会把你上一条的内容清掉，所以发一轮完整的就行，
  不用管之前补到哪儿了。

──────────────────
③ 修正：DX 谱的 touch / break 数字
──────────────────
DX 谱（带 touch 音符的那种）里，「touch」和「break」两个数
之前读反了，导致理论分和 break 扣分算错。已经修正。

标准谱（SD，没有 touch 的那种）一直是对的，不受影响。

例 /songdata 10070 紫 的物量：tap 377 / hold 99 / slide 26 /
touch 302 / break 125 —— 修正前会把后两个读反。

──────────────────
④ 新指令：/估分 —— 反推一份能上传的判定分布
──────────────────
给一个目标百分比，Liz 解出一份**游戏能接受**的判定分布
（tap / hold / slide / touch / break 各自的 CP / P / Great / Good / Miss
数量），连同 achievement、deluxscoreMax、comboStatus、maxCombo
一起给你 —— 出图和 /songdata 一样。

  /估分 <歌曲id> <难度> <百分比> [星级] [combo]

例：
  /估分 147 紫 100.0 2      → 实际 100.0019%，2 星
  /估分 147 紫 99.0 3       → 3 星
  /估分 147 紫 100.0 2 FC+  → 顺手凑成 FC+

※ 结果只会**比你要的高**（向上估计），差不超过 0.1%。
※ 星级省略就是 0（不限）；难度写 0-4 或 绿/黄/红/紫/白。

combo 可以指定连击等级：

  0 / 无 · FC · FC+ · AP · AP+

注意 **AP 和 AP+ 会忽略百分比和星级**。这两个档位一旦定死，
达成率就只由谱面物量决定了（最高 101%），所以 Liz 直接给你
该档位下最好的一份，而不是硬凑你给的那个数。

还有一个偷懒写法：x小 / x小P

  /估分 147 紫 3小     → AP，其中 break 有 3 颗小 P

写成几就是几，达成率、DX 分、星级会一起被定下来（0小 就是全 CP = 101%）。
位置随便放，写在百分比那格也行。

※ 生成的分布保证是**游戏里打得出来**的：例如 slide 没有「小 P」
  这种判定，Liz 就不会把它安排进去。

──────────────────
指令一览
──────────────────
Liz 会这些指令：
/help — 查看指令列表
/hello — 打个招呼
/random [最小] [最大] — 掷骰子；给一组值则随机选一个
/id <歌曲id> — 按 id 查歌（别名：id查歌 / songid）
/songdata <歌曲id> <难度> — 查该谱面各判定的扣分（难度 0-4 或 绿/黄/红/紫/白）
/估分 <歌曲id> <难度> <百分比> [星级] [combo] — 反推一份能上传的判定分布（别名：estimate）
/bm <别名> — 按别名查歌（别名：别名查歌 / songalias）
/name <歌名> — 按歌名查歌（别名：歌名查歌 / songname）
/song <歌名或别名> — 按歌名或别名查歌
/查询别名 <歌名或别名> — 查这首歌的别名（别名：cbm）

有问题、或者想加什么指令，在群里说一声就行～

源码（AGPL-3.0）：https://github.com/byuexzero/liz_bot
