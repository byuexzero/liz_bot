# QQ 机器人配置模板（AI 变体 liz_bot/qqgroup-ai-bot.py 的本地回退）
#
# 首选方式是**环境变量**，不需要本文件：
#     QQ_BOT_APPID=<你的 AppID>
#     QQ_BOT_SECRET=<你的 AppSecret>
#
# 若本地想沿用 YAML，复制后填入真实值：
#     cp liz_bot/config/bot-config.example.yaml liz_bot/config/bot-config.yaml
# 优先级：**环境变量 > 本文件**，只补齐环境变量中缺失的字段。
#
# 真实 bot-config.yaml 已在 .gitignore 中，不会入库。

appid: ""
secret: ""
