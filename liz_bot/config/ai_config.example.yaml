# 千帆（文心）AI 接口配置模板 —— 本地开发回退用
#
# 首选方式是**环境变量**，不需要本文件：
#     QIANFAN_ACCESS_KEY=<你的 Access Key>
#     QIANFAN_SECRET_KEY=<你的 Secret Key>
#
# 若本地想沿用 YAML，复制后填入真实值：
#     cp liz_bot/config/ai_config.example.yaml liz_bot/config/ai_config.yaml
# 优先级：**环境变量 > 本文件**。
#
# 留空是允许的：此时 AI 功能不可用，但机器人本身仍能正常启动
# （liz_bot/qqgroup-ai-bot.py 不会因缺凭据而拒绝启动）。
#
# 真实 ai_config.yaml 已在 .gitignore 中，不会入库。

ACCESS_KEY: ""
SECRET_KEY: ""
